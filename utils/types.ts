// <======== Exporting Types ========>
export type GradientButtonProps = {
    title: string,
    handlePress: () => void,
};

export type GradientViewProps = {
    isActive: boolean,
};

export type TabsBarIconProps = {
    focused: boolean,
    title: string,
};